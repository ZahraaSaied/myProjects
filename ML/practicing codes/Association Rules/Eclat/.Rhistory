setwd("/media/zahraa/venus/@current work/Machine_Learning_A-Z_Hands-On_Python_and_R_In_Data_Science/my practice/Association Rules/Eclat")
dataset = read.csv("Market_Basket_Optimisation.csv", header = FALSE)
library(arules)
dataset = read.transactions("Market_Basket_Optimisation.csv", sep = ',', rm.duplicates = TRUE)
View(dataset)
itemFrequencyPlot(dataset, topN = 20)
summary(dataset)
rules = eclat(data = dataset, parameter = list(support = 0.003))
rules = eclat(data = dataset, parameter = list(support = 0.004))
rules = eclat(data = dataset, parameter = list(support = 0.004, minlen = 2))
rules = eclat(data = dataset, parameter = list(support = 0.004, minlen = 2))
inspect(sort(rules, by = 'support')[1:10])
